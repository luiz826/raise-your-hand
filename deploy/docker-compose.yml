# Single-VPS deployment: Node backend + Kokoro TTS + Whisper STT behind Caddy
# (automatic HTTPS). One command: `docker compose --env-file deploy/.env -f deploy/docker-compose.yml up -d --build`
# Run from the repo root. Fill in deploy/.env first (see deploy/.env.example).

services:
  backend:
    build:
      context: ..
      dockerfile: deploy/Dockerfile.backend
    env_file: [.env]
    environment:
      HOST: "0.0.0.0"
      PORT: "8787"
    volumes:
      - ../data:/app/data          # pre-ingested course maps live here (must be writable by uid 1000: chown -R 1000:1000 data)
    expose: ["8787"]
    mem_limit: 1g
    restart: unless-stopped

  tts:
    build:
      context: ..
      dockerfile: deploy/Dockerfile.voice
    command: ["python", "tts/server.py"]
    env_file: [.env]
    environment:
      RYH_TTS_PORT: "8788"
      RYH_VOICE_HOST: "0.0.0.0"
    volumes:
      - voice-models:/app/tts/models
      - hf-cache:/home/app/.cache/huggingface
    expose: ["8788"]
    mem_limit: 512m
    restart: unless-stopped

  stt:
    build:
      context: ..
      dockerfile: deploy/Dockerfile.voice
    command: ["python", "stt/server.py"]
    env_file: [.env]
    environment:
      RYH_STT_PORT: "8789"
      RYH_VOICE_HOST: "0.0.0.0"
    volumes:
      - voice-models:/app/tts/models
      - hf-cache:/home/app/.cache/huggingface
    expose: ["8789"]
    mem_limit: 512m
    restart: unless-stopped

  caddy:
    build:
      context: ..
      dockerfile: deploy/Dockerfile.caddy   # Caddy + rate-limit plugin
    env_file: [.env]                 # provides RYH_DOMAIN for the Caddyfile
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - ./Caddyfile:/etc/caddy/Caddyfile:ro
      - ./site:/srv/site:ro
      - caddy-data:/data
      - caddy-config:/config
    depends_on: [backend, tts, stt]
    restart: unless-stopped

volumes:
  voice-models:
  hf-cache:
  caddy-data:
  caddy-config:
