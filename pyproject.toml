[project]
name = "raise-your-hand-voice"
version = "0.1.0"
description = "OpenAI STT + TTS proxy servers for the Raise Your Hand extension (stdlib only)."
requires-python = ">=3.11"
dependencies = []

# Not a packaged library — just a managed environment for the two voice servers.
[tool.uv]
package = false
